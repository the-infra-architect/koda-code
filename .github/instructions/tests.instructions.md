---
applyTo: "tests/**/*.py"
---

Test observable behavior, boundaries, failures, and regressions. Keep tests deterministic and
readable. Use temporary repositories and files; never depend on network services, global Git state,
or execution order.
