# Claim Register

Claims are research evidence, not automatic implementation rules.

| ID | Claim | Sources | Confidence | Context / caveat | Candidate Koda implication | Status |
|---|---|---|---|---|---|---|
| C001 | Software quality is multidimensional; Koda must not reduce quality to one simplicity/complexity score. | S001, S002, S003 | A/B | Different quality attributes can conflict. | Profile relevant quality attributes before architecture. | supported |
| C002 | Architecture should be the least complex design that satisfies the important quality attributes and constraints of the actual project. | S002, S003, S005 | B | Complexity can be justified by scale, reliability, ownership, security, compatibility, or other requirements. | Prefer proportional engineering, not minimal component count. | supported |
| C003 | Maintainability and modifiability are architectural concerns, not only formatter/style concerns. | S004, S005 | B | Exact structural tactics depend on the codebase. | Reviewer should reason about coupling/cohesion/change cost when relevant. | supported |
| C004 | Review should consider design, functionality, complexity, tests, naming, documentation, and maintainability. | S006 | B | Review process details vary. | Keep Reviewer broad but evidence-focused. | supported |
| C005 | Review should challenge unnecessary complexity/speculative functionality but should not demand theoretical perfection. | S006, S007, S009 | B | Critical systems may need stricter acceptance. | Separate blocking defects from optional improvements. | supported |
| C006 | Short-lived, focused changes generally improve reviewability, rollback, and integration. | S008, S025 | B | Some migrations/refactors are necessarily larger. | Keep Koda mission branches/worktrees scoped and short-lived. | supported |
| C007 | Readable code favors clarity, descriptive names, project/ecosystem consistency, and abstractions that map to the problem. | S009, S010 | B | Language idioms differ. | Prefer existing project conventions over Koda-specific style. | supported |
| C008 | There is insufficient evidence for universal hard limits on function size, class size, interface implementation count, or cognitive/cyclomatic complexity. | S006, S009 | C | Metrics can still be useful signals. | Do not encode arbitrary hard structural thresholds unless project policy supplies them. | supported inference |
| C009 | Documentation should be accurate, useful, proportional, and updated with behavior/setup changes. | S011 | B | Public/regulatory contexts may need more. | Avoid generating large boilerplate docs for tiny changes. | supported |
| C010 | Test strategy should be risk/context driven; no universal test count or coverage percentage establishes sufficiency. | S012, S013, S014, S015 | B | Project/org policy may impose thresholds. | Build test plan from behavior/risk and preserve existing thresholds. | supported |
| C011 | Unit, integration, E2E, and nonfunctional tests cover different risks; E2E should focus on critical journeys rather than replace lower-level tests. | S012, S013 | B | Taxonomy varies by ecosystem. | Resolve test layers from architecture and risk. | supported |
| C012 | Automated tests should be fast/reliable where practical; flaky tests erode trust and should not be normalized. | S012, S015 | B | Some expensive suites run less often. | Koda should distinguish fast merge gates from slower optional/scheduled checks. | supported |
| C013 | Coverage is useful as a gap/risk signal but is not proof of code quality. | S014, S015 | B | Explicit project thresholds still matter. | Never invent a universal Koda coverage percentage. | supported |
| C014 | Mutation testing has demonstrated value for finding test weakness but should be selective due cost/complexity. | S016 | B/C | Best for critical logic/mature suites or existing tooling. | Do not add mutation testing to every repo. | supported |
| C015 | Security practices should be integrated into normal development rather than appended only at release. | S017, S018 | A | Specific controls depend on threat surface/risk. | Security belongs in engineering profile and quality contract. | supported |
| C016 | Security verification techniques should be selected based on applicability: threat modeling, tests, static scanning, secret checks, fuzzing, web scanners, and included-component analysis are not equally relevant everywhere. | S018, S020 | A/B | NIST explicitly scopes some techniques such as web scanners as applicable-only. | Build a threat-surface resolver. | supported |
| C017 | Web application security is stronger when expressed as testable requirements rather than vague 'secure code' instructions. | S019 | A/B | ASVS is web/service scoped. | Use ASVS-informed checks for exposed web systems where appropriate. | supported |
| C018 | Secrets should not be hard-coded or logged; sophistication of secret storage should match the environment. | S021 | A/B | Local tools may use OS/user config rather than enterprise vaults. | Never compensate for missing infrastructure by committing secrets. | supported |
| C019 | Software component/supply-chain controls should be risk based; Koda must not invent legal/license/risk-acceptance policy. | S022, S031 | A/B | Acceptability can be organization-specific. | Detect/report component risk; respect provided organization policy. | supported |
| C020 | AI-generated code and AI self-reports require independent deterministic verification. | S023, S024 | A/B | Do not generalize study defect percentages. | Git/filesystem/tests/static checks remain sources of truth. | supported |
| C021 | Continuous delivery means keeping software releasable and does not require automatic production deployment. | S026 | B | Some products have no hosted deployment at all. | Koda may stop at VERIFIED/READY TO FINISH. | supported |
| C022 | Artifacts needed to reproduce software—code, tests, build/config, migrations, and relevant agent/prompt config—should be versioned or otherwise traceable. | S027 | B | Large binaries may live outside Git. | Avoid undocumented machine-local build magic. | supported |
| C023 | Dependency state and build/install behavior should be traceable/reproducible using the project's own ecosystem mechanisms. | S028, S047, S048, S049, S050, S051 | B | Different ecosystems have different lock/build semantics. | Detect and preserve package manager/lock strategy. | supported |
| C024 | Third-party GitHub Actions can be security-sensitive; immutable pinning is a GitHub-specific strong practice when generating workflows. | S030 | A | GitHub Actions-specific; project/org policy may control updating. | Do not generalize GitHub action mechanics to other CI systems. | supported |
| C025 | Static/dependency security tools are capability- and language-dependent; their absence may reflect unsupported ecosystem or platform plan, not engineering negligence. | S031, S032 | A | Other tools may fill the role. | Resolve security capability rather than assume one named service. | supported |
| C026 | Performance/resource efficiency should be considered early enough to avoid obvious bad designs, but complex optimization should be driven by realistic targets and measurement. | S036 | B | Measurement strategy varies. | Fix obvious N+1/O(n²)/materialization issues; profile before caches/queues/concurrency. | supported |
| C027 | Retries belong only around likely transient failures and should be bounded with timeout/backoff/idempotency awareness. | S037 | B | Remote/distributed dependencies only. | Do not create generic retry wrappers for every failure. | supported |
| C028 | Observability should be proportional and actionable; not every program needs logs+metrics+traces. | S035 | B/C | Services and long-running systems need more operational signals than one-off tools. | Derive observability from runtime/operational lifecycle. | supported |
| C029 | Persistent valuable data requires recovery/backup consideration proportional to loss impact. | S045 | A/B | Mechanism is datastore-specific. | Storage choice includes recovery, not only query API. | supported |
| C030 | Schema changes in live databases can impose locking/availability risk and should be treated differently from ordinary source edits. | S046 | A/C | Evidence is PostgreSQL-specific; exact semantics vary. | Preserve existing migration tool and assess data/concurrency/downtime. | supported |
| C031 | DuckDB is optimized for embedded analytical/OLAP workloads and is not a universal transactional database. | S038, S041 | A | DuckDB capabilities are evolving. | Prefer it when workload matches; do not force it from personal preference. | supported |
| C032 | Native read-write DuckDB databases should not be placed on NAS/NFS/SMB/Samba by default. | S040 | A | Read-only remote data, cloud block disks, DuckLake, and future versions differ. | Shared-folder requirement must trigger storage topology reasoning. | supported |
| C033 | Direct shared database files on network filesystems carry locking/sync risks beyond DuckDB; SQLite documentation independently warns about this architecture. | S042, S043 | A | Exact failure modes vary by filesystem/database. | Prefer central application/server ownership for shared multi-machine writes. | supported |
| C034 | Multi-writer transactional concurrency is an architectural requirement; client/server databases such as PostgreSQL explicitly support multi-session concurrency controls. | S042, S044 | A | PostgreSQL is one candidate, not a universal answer. | Ask/derive writer topology and consistency needs before datastore selection. | supported |
| C035 | Existing monorepo graph/affected tooling should normally be reused instead of Koda inventing a second dependency graph. | S058 | B/C | Shared config/lock changes can broaden impact. | Use Nx/Turbo/native workspace mechanisms first, widen conservatively when needed. | supported |
| C036 | Web UI accessibility should use testable standards and native semantics where possible. | S059, S060 | A | Web UI-specific. | UI/UX agent must cover accessibility/interaction, not just visual polish. | supported |
| C037 | Questions for nontechnical users should use language they understand, focus on outcomes, and request only information that is genuinely needed. | S061, S062 | B | Service-design source should later be validated with Koda user studies. | Translate technical uncertainty into product-language questions. | supported |
| C038 | Existing project conventions/configuration should normally outrank Koda preferences. | S047, S049, S050, S052, S053, S054, S055, S056, S057 | B | Broken/unsafe tooling or explicit migration requests are exceptions. | Inspect repo and native lifecycle before adding/replacing tools. | supported |
| C039 | No universal quality toolchain is appropriate across Python, JS/TS, JVM, .NET, Rust, Go, monorepos, local apps, and services. | S047, S049, S050, S053, S055, S056, S057 | A/B | Organization-wide policies can intentionally standardize. | Model quality capabilities, then resolve through project evidence. | supported |
| C040 | Strict typing can improve guarantees, but globally tightening type policy can be a migration and should not be forced casually. | S052 | A/C | TypeScript-specific authoritative evidence; analogous behavior varies by language. | Prefer useful specific types in touched code while preserving existing project policy. | supported |
| C041 | Quality gates are project policies; Sonar's changed/new-code philosophy is useful, but its numeric defaults are not universal. | S033, S034 | B | If an organization mandates Sonar thresholds, preserve them. | Make Sonar optional/capability-driven. | supported |
| C042 | A strong local deterministic quality contract can exist without hosted CI. | S021, S022, S026, S027 | C | Synthesis rather than direct quote. | Do not make GitHub/cloud access a precondition for good engineering. | supported inference |
| C043 | Greenfield and existing-project work need different decision behavior. | S002, S038, S047, S050 | C | Synthesis from architecture and ecosystem evidence. | Greenfield chooses; adoption preserves/understands first. | supported inference |
| C044 | A file/tool marker proves that a capability exists, not that every new component should use it. | S047, S050, S058 | C | Inference. | Detection and recommendation must remain separate. | supported inference |
| C045 | Koda should prefer project-native commands/scripts over guessed raw commands because configuration/plugins can materially change lifecycle behavior. | S047, S049, S050, S053, S055, S056, S057 | B | Fallback discovery is still needed when scripts are absent. | Treat existing scripts/wrappers as highest-quality execution evidence. | supported |
| C046 | Public compatibility surfaces require explicit change-impact reasoning; improving style or guideline compliance is not sufficient reason to break existing consumers. | S064, S065, S066 | A/B | What counts as a public/stable contract is project-specific. | Detect API/library/CLI/config/data-format contracts and flag likely breaking changes; preserve existing contract policy. | supported |
| C047 | Semantic Versioning is useful only when the project defines a public API and chooses SemVer; Koda must not impose SemVer universally. | S063 | A | Projects may use calendar versioning, date versions, internal build numbers, or no formal public versioning. | Respect existing version scheme; use SemVer semantics only when declared or newly selected for a compatible project. | supported |
| C048 | Behavior, error contracts, required parameters, authorization requirements, and performance/rate-limit expectations can be compatibility surfaces—not only function names or endpoint URLs. | S064, S065 | A/B | Exact compatibility definition should be project-owned. | Reviewer should consider behavioral/API contract regressions, not just compilation. | supported |
| C049 | Deprecation/support windows are product policy and should not be invented by Koda. | S065, S066 | A/C | Different projects use radically different stability tracks and support periods. | Require/document migration path when project policy says so; never copy GitHub/Kubernetes support durations as defaults. | supported |
| C050 | Reproducible dependency resolution, SBOM inventory, and build provenance/attestation are distinct supply-chain capabilities. | S067, S068, S069 | A | Each has different cost and applicability. | Model them separately; do not treat a lockfile as an SBOM or an SBOM as provenance. | supported |
| C051 | Formal SBOM/provenance capabilities are most justified for distributable artifacts, organizational/security requirements, or higher-risk supply chains; they need not be a universal local-project gate. | S067, S068, S093 | B/C | Some organizations/regulations may require them regardless of size. | Make SBOM/provenance conditional capabilities driven by release/distribution/risk/policy. | supported inference |
| C052 | CI/CD workflows are privileged code and should follow least privilege, secret hygiene, untrusted-input handling, and pipeline-integrity controls. | S070, S072, S073, S074, S096 | A/B | Mechanics differ by CI provider. | Generated/modified CI is a security-sensitive change surface and should receive dedicated validation. | supported |
| C053 | When GitHub Actions must authenticate to a compatible cloud/service, short-lived OIDC credentials can reduce reliance on stored long-lived credentials. | S071 | A | Requires compatible provider, configuration and authorization; local/offline projects may not use it. | Prefer OIDC when the existing deployment platform supports it, but never make it a universal deployment requirement. | supported |
| C054 | Dependency pinning improves reproducibility/integrity but must be paired with an update process; pinning without maintenance can preserve vulnerable or obsolete versions. | S070, S092 | B | Exact pin semantics vary by ecosystem and artifact type. | If Koda strengthens pinning, it should preserve/create a reasonable update path where applicable rather than freeze dependencies indefinitely. | supported |
| C055 | Applications/end products and reusable libraries can legitimately have different dependency-locking semantics. | S089, S090, S091 | A/C | Evidence is ecosystem-specific; do not generalize exact Cargo behavior to Python/npm/etc. | Determine package role and ecosystem before changing dependency ranges or lockfiles. | supported |
| C056 | Secure defaults should reduce the need for nontechnical users to discover and enable basic security controls themselves. | S075, S077, S093 | A/B | What the secure default is depends on product functionality and threat model. | For beginner-facing software, prefer safe default paths and explain deliberate relaxations instead of shipping insecure defaults plus hardening instructions. | supported |
| C057 | For greenfield, security-sensitive or exposed low-level code where language choice is genuinely open, memory-safe languages should receive positive weight; existing memory-unsafe code should not be rewritten automatically without a justified migration. | S076 | A/C | Feasibility, ecosystem, performance, platform and organizational constraints still matter. | Add memory-safety as a quality attribute for relevant greenfield/system components, not as a blanket language preference. | supported |
| C058 | Deploy-varying configuration and credentials should be separated from source code, but environment variables are one mechanism rather than a universal answer. | S078, S081 | B | Desktop/local/mobile/embedded systems may use secure config files, OS stores or other mechanisms. | Externalize secrets/deploy config using the platform's existing appropriate mechanism; do not force env vars everywhere. | supported |
| C059 | Potentially untrusted input should be validated at a trusted boundary for both syntax and business semantics, using framework/schema mechanisms where available rather than ad-hoc filters. | S079 | B | Validation strategy depends on input type; free-form text cannot always be narrow-allowlisted. | Security resolver should identify untrusted input boundaries and prefer framework/schema validation. | supported |
| C060 | Errors and logs should support debugging/operations without exposing secrets, sensitive data, internal stack details to untrusted clients, or creating unbounded logging risk. | S080 | B | Logging/audit requirements vary by system and regulation. | Review error/log behavior proportionally; translate user-facing errors while preserving protected technical diagnostics. | supported |
| C061 | When using a capable relational database, durable invariants such as uniqueness, nullability and referential relationships are often stronger when enforced with database constraints rather than application checks alone. | S083 | A/C | Not every business rule belongs in a database constraint and not every datastore supports the same features. | Consider datastore constraints for persistent invariants; avoid duplicating or contradicting existing schema rules. | supported |
| C062 | Related persistent mutations that must succeed or fail together should use an appropriate transaction boundary; transactions should not be made larger than integrity requires. | S082 | A/C | Transactional capabilities differ by datastore and distributed boundary. | Identify atomicity requirements instead of scattering multi-step writes or wrapping unrelated work in giant transactions. | supported |
| C063 | Asynchronous messaging, duplicate delivery and retries introduce idempotency/order/consistency requirements that must be designed explicitly. | S084, S088, S094, S095 | B | Only relevant once asynchronous/distributed patterns actually exist. | Do not add queues/events casually; when they are justified, add corresponding correctness semantics. | supported |
| C064 | Microservices can provide independent scaling/deployment/isolation benefits but add distributed-system, testing, consistency, observability, governance and operational complexity. | S085, S086 | B | Team/organization architecture is an explicit requirement in many real systems. | Microservices require concrete benefits/readiness evidence; a modular monolith or existing monolith may be the better proportional choice. | supported |
| C065 | Caching is not a free optimization: it introduces staleness, invalidation, expiration, memory/capacity and multi-instance consistency concerns. | S087 | B | Some frameworks/services provide transparent caching semantics. | Introduce cache only for concrete access-pattern/performance needs and define freshness/invalidation behavior. | supported |
| C066 | When rolling deployments or zero-downtime requirements cause old and new application versions to overlap, schema evolution should favor backward-compatible staged changes rather than one-step destructive changes. | S097 | B | Not necessary for offline/single-instance systems that can tolerate coordinated downtime. | Quality profile should select staged expand/migrate/contract behavior only when deployment topology requires it. | supported |
| C067 | Generated CI must treat PR/issue/branch metadata and other contributor-controlled values as untrusted data; direct interpolation into executable scripts can create injection vulnerabilities. | S073, S096 | A | GitHub Actions-specific syntax; general principle is broader. | Add CI-workflow static validation for untrusted expression-to-shell/script flows when GitHub Actions is generated/modified. | supported |
| C068 | Secure framework/language building blocks that eliminate vulnerability classes are preferable to repeated custom defensive code when they fit the project. | S075, S093 | B | Existing frameworks and project constraints still govern the concrete choice. | Prefer parameterized queries, framework escaping/validation/auth mechanisms, and safe libraries over bespoke security machinery. | supported |
| C069 | A vulnerability in a dependency inventory does not automatically mean the specific product is exploitable, but non-applicability requires evidence rather than agent assertion. | S098, S099 | B | Scanner false positives/context gaps exist; known affected/exploitable findings still require timely action according to policy/risk. | Support vulnerability triage states and evidence; never let a model silently dismiss a finding as 'not applicable'. | supported |
| C070 | Vulnerability severity thresholds and dependency license allow/deny policy are project/organization policy, not universal Koda constants. | S100 | A/C | Some regulations or organizational security baselines may define mandatory thresholds. | Preserve existing configured thresholds; if none exist, report risk and recommend rather than invent a universal fail level. | supported |
| C071 | Security tool absence or unsupported ecosystem coverage must not be interpreted as a passing security result. | S032, S100 | A/C | Other local/project tools may provide equivalent coverage. | Quality capability state should distinguish `unavailable` from `pass` and seek existing alternatives where appropriate. | supported inference |
| C072 | License metadata can be surfaced and enforced when a project supplies a policy, but Koda should not make independent legal-license acceptability decisions. | S100 | A/C | License compatibility is legal/organizational context. | Report licenses and respect explicit allow/deny policy; otherwise avoid hard legal conclusions. | supported |